# Oracle RAG System Dependencies
# Integrates Grok RAG + Graph RAG with your-pattern infrastructure

# ============================================================================
# Core RAG Components
# ============================================================================
sentence-transformers>=2.2.2      # Embedding generation (all-MiniLM-L6-v2)
langchain>=0.1.0                  # Text splitting and chunking
faiss-cpu>=1.8.0                  # Local vector search (fallback to Milvus)

# ============================================================================
# Graph RAG Dependencies
# ============================================================================
transformers>=4.35.0              # NER and relation classification
torch>=2.1.0                      # PyTorch backend for transformers
neo4j>=5.14.0                     # Neo4j Python driver
pymilvus>=2.3.3                   # Milvus vector database client

# ============================================================================
# Grok RAG Document Ingestion Dependencies
# ============================================================================
PyPDF2>=3.0.1                     # PDF text extraction
pdfplumber>=0.10.3                # PDF table extraction
python-docx>=1.1.0                # DOCX parsing
openpyxl>=3.1.2                   # Excel (.xlsx) parsing
pytesseract>=0.3.10               # OCR for scanned PDFs
Pillow>=10.1.0                    # Image processing for OCR
pandas>=2.1.3                     # DataFrame handling (tables, CSV)
python-magic>=0.4.27              # File type detection

# ============================================================================
# LLM Integration (Multi-provider)
# ============================================================================
openai>=1.3.5                     # OpenAI API (GPT-4) - fallback
google-cloud-aiplatform>=1.38.0   # Vertex AI (Gemini) - fallback
# Note: Grok API uses OpenAI-compatible client

# ============================================================================
# Utility Libraries
# ============================================================================
python-dotenv>=1.0.0              # Environment variable management
pydantic>=2.5.0                   # Data validation and settings
aiohttp>=3.9.1                    # Async HTTP client
requests>=2.31.0                  # HTTP client
tqdm>=4.66.1                      # Progress bars

# ============================================================================
# Testing & Validation
# ============================================================================
pytest>=7.4.3                     # Testing framework
pytest-asyncio>=0.21.1            # Async test support
pytest-cov>=4.1.0                 # Code coverage

# ============================================================================
# Monitoring & Logging
# ============================================================================
loguru>=0.7.2                     # Enhanced logging (optional, config uses stdlib)

# ============================================================================
# System Requirements
# ============================================================================
# External dependencies (install via apt/brew):
# - tesseract-ocr (for pytesseract)
# - libmagic1 (for python-magic)
#
# Installation commands:
# Ubuntu/Debian: sudo apt-get install tesseract-ocr libmagic1
# macOS: brew install tesseract libmagic
